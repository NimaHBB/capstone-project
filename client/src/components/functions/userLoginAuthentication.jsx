import {
  getAuth,
  createUserWithEmailAndPassword,
  signInWithEmailAndPassword,
} from "firebase/auth";

const UserLoginAuthentication = () => {
  const auth = getAuth();
};

export default UserLoginAuthentication;
