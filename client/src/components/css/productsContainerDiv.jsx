import styled from "styled-components";

const ProductsContainerDiv = styled.div`
  display: flex;
  direction: column;
  justify-content: center;
  flex-wrap: wrap;
  background: transparent;
  width: 100%;
`;

export default ProductsContainerDiv;
